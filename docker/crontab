# supercronic crontab — schedules the sync twice a week.
# Times are local (America/Sao_Paulo), set via CRON_TZ + the TZ env var.
CRON_TZ=America/Sao_Paulo

# Segunda e quinta, 06:30 (horário de Brasília).
30 6 * * 1,4 node /app/dist/index.js
